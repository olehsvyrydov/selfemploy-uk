broken=74
tests=886

What the nightly e2e run is measured against.

  broken  The number of e2e-tagged tests known to be failing. The run fails only if the count
          exceeds this, so the suite cannot rot further while it is being repaired.
  tests   How many tests a whole-suite run produces. Checked as well as the failure count, because
          a run that never happened reports zero failures and would otherwise look like a clean
          sweep. A count far above it means reports from an earlier run are being counted too.

Current readings, 886 tests:

  73   CI
  74   this baseline: the highest expected reading, plus nothing

`broken` is set from CI, where it is enforced, and carries one test of headroom over the highest
reading seen there. Two runs of the same tree once differed by one, so at least one test is flaky,
and a ratchet that trips on a coin flip gets ignored like any other false alarm - which is how this
suite reached its current state. Finding that flaky test would let this drop.

How it got here, since the numbers only make sense in order (all on 2026-07-28):

  224 / 883   CI on xvfb-run defaults
  225 / 883   CI with 24-bit colour - so the depth was not the problem
   75 / 883   CI once a window manager was running. None of that difference was the tests: without
              one a stage never takes focus, so TestFX finds no nodes. Never calibrate from a
              broken display.
   76, 77     the same commit on a developer desktop, twice - the flake
   74 / 882   one test deleted rather than fixed: it saved records, reset the in-memory database and
              asserted they survived, which cannot happen. The claim is now covered for real by
              SubmissionPersistenceTest, against a file.
   73 / 882   the declaration tests taught to scroll to the checkbox they click.

That last change fixes seven tests on a desktop but only one here, because CI does not honour the
window size a test asks for - a stage asking for 700px wide measures 900 - so a click lands
somewhere other than the control rather than failing to find it. Several of the remaining failures
look like the same cause and would likely go together if it were solved.

Lower `broken` in the same commit that fixes tests. Never raise it.
